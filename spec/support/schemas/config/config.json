{
  "type": "object",
  "properties": {
    "rabbit": {
      "type": "hash",
      "properties": {
        "host": { "type": "string" },
        "port": { "type": "integer" },
        "vhost": { "type": "string" },
        "user": { "type": "string" },
        "pass": { "type": "string" },
        "admin_pass": { "type": "string" }
      },
      "required": [
        "host",
        "port",
        "vhost",
        "user",
        "pass",
        "admin_pass"
      ]
    },
    "queues": {
      "type": "hash",
      "properties": {
        "queue_from": { "type": "string" },
        "queue_to": { "type": "string" },
        "admin": {
          "type": "hash",
          "properties": {
            "queue_from": { "type": "string" },
            "queue_to": { "type": "string" }
          },
          "required": [
            "queue_from",
            "queue_to"
          ]
        }
      },
      "required": [
        "queue_from",
        "queue_to",
        "admin"
      ]
    },
    "janus": {
      "type": "hash",
      "properties": {
        "plugins": { "type": "hash" }
      },
      "required": [
        "plugins"
      ]
    },
    "gem": {
      "type": "hash",
      "properties": {
        "log": {
          "type": "hash",
          "properties": {
            "level": { "type": "string" }
          },
          "required": [
            "level"
          ]
        },
        "session": {
          "type": "hash",
          "properties": {
            "keepalive": { "type": "integer" }
          },
          "required": [
            "keepalive"
          ]
        },
        "thread": {
          "type": "hash",
          "properties": {
            "enable": { "type" : "boolean" },
            "number": { "type": "integer" }
          },
          "required": [
            "enable",
            "number"
          ]
        }
      },
      "required": [
        "log",
        "session",
        "thread"
      ]
    }
  },
  "required": [
    "rabbit",
    "queues",
    "janus",
    "gem"
  ]
}
